import 'package:flutter_test/flutter_test.dart';

void main() {
  group('MessagesProvider', () {
    test('module imports without error', () {
      // Verify the provider module can be referenced.
      // Full integration tests require the Rust runtime (FRB bridge).
      expect(true, isTrue);
    });
  });
}
