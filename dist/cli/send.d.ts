/**
 * `burrow send` — Send an encrypted message to a group.
 */
export declare function sendCommand(opts: {
    groupId: string;
    message: string;
    keyPath?: string;
    dataDir?: string;
}): Promise<void>;
//# sourceMappingURL=send.d.ts.map